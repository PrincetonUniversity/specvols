% FILTER_TYPE_GAUSSIAN Filter type a Gaussian filter
%
% Usage
%    filter_type = filter_type_gaussian();
%
% Output
%    filter_type: The filter type value corresponding to a Gaussian filter.

% Author
%    Joakim Anden <janden@flatironinstitute.org>

function filter_type = filter_type_gaussian()
    filter_type = 3;
end
