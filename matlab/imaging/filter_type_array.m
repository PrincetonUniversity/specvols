% FILTER_TYPE_ARRAY Filter type for an array filter
%
% Usage
%    filter_type = filter_type_array();
%
% Output
%    filter_type: The filter type value corresponding to an array filter.

% Author
%    Joakim Anden <janden@flatironinstitute.org>

function filter_type = filter_type_array()
    filter_type = 4;
end
