% FILTER_TYPE_IDENTITY Filter type for identity filter
%
% Usage
%    filter_type = filter_type_identity();
%
% Output
%    filter_type: The filter type value corresponding to the identity filter.

% Author
%    Joakim Anden <janden@flatironinstitute.org>

function filter_type = filter_type_identity()
    filter_type = 0;
end
